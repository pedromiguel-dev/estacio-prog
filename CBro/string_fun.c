#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
    char first_name[] = "Pedro";
    char last_name[] = "Miguel";

    /* strlwr(first_name);
    strupr(last_name); // converts to uppercase
    strcat(first_name, last_name); // concatenates last_name to first_name
    strncat(first_name, last_name, 3); // concatenates the first 3 characters of last_name to first_name
    strcpy(first_name, last_name); // copies last_name to first_name
    strncpy(first_name, last_name, 3); // copies the first 3 characters of last_name to first_name 
    strrev(first_name); // reverses the string
    */

    return 0;
}
